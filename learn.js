const a = 'git'

const b = 'learn'

const c = b + a 

const d = 'git status'

const e = 'diff'

const f = 'back'

const str = '需要提交的文件修改通通放到暂存区，然后，一次性提交暂存区的所有修改'

const merge = 'dev'

const branch = 'master branch'

const branchName = 'master'

function fixBug(a) {
	return a++
}